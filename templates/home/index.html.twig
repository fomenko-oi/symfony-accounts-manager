{% extends 'base.html.twig' %}

{% block title %}Home page{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <div class="alert alert-primary">
            You are authenticated: #{{ user.id }}: <span class="badge badge-success">{{ user.username }}</span>
        </div>
    </div>
{% endblock %}
