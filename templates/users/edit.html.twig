{% extends 'base.html.twig' %}

{% block title %}Updating user #{{ user.id }}{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Editing user #{{ user.id }}</h1>

        {{ form(form) }}
    </div>
{% endblock %}
